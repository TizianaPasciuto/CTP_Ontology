PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
SELECT(Count (distinct *) as ?count)  WHERE {
  ?sub a ?type .
  filter(regex(STR(?type),"^https://w3id.org/ctp#", "i"))
} 
#Group By ?type
#Order by  DESC(?count)